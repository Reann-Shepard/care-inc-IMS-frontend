import InventoryHeader from '@/components/inventory/inv_table-header';

export const metadata = {
  title: 'Inventory',
};

export default function InventoryPage() {
  return (
    <div>
      <InventoryHeader />
    </div>
  );
}
